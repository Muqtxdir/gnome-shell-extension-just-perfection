#!/usr/bin/make -f
#export DH_VERBOSE = 1

%:
	dh $@ --buildsystem=meson

override_dh_install:
	# delete schema-directory.
	rm -r debian/tmp/usr/share/gnome-shell/extensions/just-perfection-desktop@just-perfection/schemas
	dh_install
